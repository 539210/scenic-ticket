# 景点售票系统稳定化计划

更新时间：2026-07-13

## 执行原则

- 严格保留 Java 21、Swing、Maven、JDBC、MySQL、MongoDB、HikariCP、JUnit 5、SLF4J + Logback、BCrypt。
- 保留课程要求的 `users`、`categories`、`items`、`orders`、`profiles` 表及其既有字段。
- 保留并实际使用 `action_logs`、`comments`、`item_details`、`system_logs` 集合。
- 每个里程碑按“读取进度 → 复现 → 失败测试 → 小范围实现 → 相关测试 → `mvn clean test` → 可用时真实集成测试 → 更新文档 → Git 检查点”执行。
- 不操作或清空本地 `scenic_ticket` 数据库；真实集成验证只使用 `scenic_ticket_test`。
- 当前分支为 `codex/scenic-ticket-stabilization`，不在 `main` 上进行长期重构。

## 里程碑

| 里程碑 | 状态 | 必需产出与退出条件 |
| --- | --- | --- |
| M0 环境和基线 | 已完成 | 完整阅读、环境与测试基线、审计文件、已知问题证据、初始 Bug 清单；不可自动点击的 Swing 项已明确列入手工验收 |
| M1 数据库安装与迁移 | 已完成 | 测试库安全初始化、Day08 升级、版本化迁移、票种/库存/退款/核销结构、对象/索引及真实 Mongo 聚合验证均通过；检查点 `8d72476` |
| M2 用户、会话和权限 | 已完成 | 服务层 actor 授权、管理员用户查询/详情/启禁/角色管理、自身和最后管理员保护、退出审计与异步会话隔离均通过；检查点 `0847777` |
| M3 分类、景点和详情 | 已完成 | 分类父级/循环校验与编辑、景点完整维护、结构化 MongoDB 详情、历史字符串 ID 兼容、独立简介展示和真实跨库回归均通过；检查点 `8e93e69` |
| M4 票种、日期和库存 | 已完成 | 多票种、未来游玩日期、每日库存、同连接事务/行锁防超卖、可售查询和管理员维护界面均通过；检查点 `4fae212` |
| M5 订单、支付和退款 | 已完成 | 待支付、主动支付、完整快照、合法状态机、过期、取消、退款与库存恢复均通过；检查点 `ff81022` |
| M6 入园核销 | 已完成 | 管理员核销、日期/状态/数量资格、部分记录、全部完成、审计和禁止核销后退款均通过；检查点 `2f15d07` |
| M7 评论和景点互动 | 已完成 | 服务层支付资格、每用户每景点唯一可修改、完整字段展示、混合 ID 聚合和真实数据库回归均通过；检查点为本次提交 |
| M8 推荐、统计和审计 | 已完成 | 推荐分/原因、热门排行跨库显示名称、用户/月报、完整审计条件组合、视图和第二存储过程实际调用均通过 |
| M9 Swing 重构与巡检 | 进行中 | 已抽出主要用户页面、完整后台、认证及全部业务表单/查询弹窗组件，完成通用组件、可移植启动和版本文档校准；继续收口展示格式化边界与 UI_ACTION_MATRIX |
| M10 真实数据库集成验证 | 待开始 | 空测试库、MySQL/MongoDB/跨库、并发、普通用户和管理员完整流程 |
| M11 答辩稳定版 | 待开始 | README/设计/手册/报告一致、五分钟演示、答辩问答、最终报告 |

## M0 当前任务

- [x] 阅读目标文件与课程要求 DOCX（课程文件实际位于项目根目录）。
- [x] 阅读 README、指定设计/手册/测试文档、POM、全部 SQL、全部主代码与测试代码。
- [x] 记录 Git、Java、Maven、数据库服务与端口基线。
- [x] 使用 Java 21 执行 `mvn clean test`。
- [x] 创建审计与控制文件。
- [x] 为四个已知问题建立自动化/数据/静态证据并记录根因或当前不可复现边界。
- [x] 完成第一轮 UI 操作矩阵静态检查；无法自动点击的项目明确保留为手工验收。
- [x] 完成初始 Bug 分级复核，确认所有 P0/P1 的修复顺序。
- [x] M0 Git 检查点提交：`[Day 09] 建立稳定化审计与基线`。

## 优先修复顺序

1. P0 服务层权限绕过与任意订单状态修改。
2. P1 测试库隔离与安全数据库迁移基础。
3. P1 多票种、日期、库存及待支付订单状态机。
4. P1 支付、取消、退款、过期、核销和事务一致性。
5. 四个已知显示/查询问题及评论资格与唯一性。
6. 管理员用户管理、审计补全和 Swing 拆分。
7. 真实数据库与完整演示流程验收。
