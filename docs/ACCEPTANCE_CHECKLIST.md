# 最终验收清单

更新时间：2026-07-14

- [ ] 1. `PLAN.md` 必需里程碑全部完成。
- [x] 2. 所有 P0、P1 Bug 已在 M10 完整真实套件复核后关闭。
- [x] 3. 四个已知 Bug 均有根因、修复和自动化/真实数据库验证；最终桌面视觉冒烟仍作为 M11 发布检查。
- [x] 4. M11 Java 21 最终默认套件通过（158 tests，0 failures，0 errors，2 skipped），完整真实套件通过（172 tests，0 failures，0 errors，1 skipped）。
- [x] 5. MySQL `scenic_ticket_test` 全新安装、Day08 旧结构升级和最终业务集成均在 M10 重跑通过。
- [x] 6. MongoDB `scenic_ticket_test` 四集合、索引、中文、兼容 ID、聚合和跨库业务在 M10 重跑通过。
- [x] 7. 景点 MySQL/MongoDB 创建、更新、兼容 ID、订单/库存/退款/评论/审计关键跨库流程均通过并清理临时数据。
- [x] 8. MySQL 全部脚本可从空测试库执行；四个 Mongo JS 脚本已用官方 `mongosh 2.9.2` 在空 `scenic_ticket_test` 顺序实跑并验证集合、样例和索引。
- [x] 9. 全新安装和 Day08 升级实跑确认课程原表、字段和历史用户/档案/订单均保留。
- [x] 10. 空库重建后两个视图已创建，并由 `ReportDAO`/真实集成测试实际查询。
- [x] 11. 两个存储过程均已实际调用；月报由统计页调用，`sp_update_inactive_items` 由 `ReportDAO` 和真实集成测试验证。
- [x] 12. 空库重建后两个触发器已创建；真实订单写入和数据库对象测试随 M10 套件通过。
- [x] 13. MongoDB 行为、评分、热门、审计等至少三个聚合管道已在 M10 真实测试库返回结果。
- [x] 14. 普通用户真实完整流程通过：注册、登录、档案、浏览、可售查询、待支付、支付、订单、评论、退款、退出和重登持久化。
- [x] 15. 管理员真实完整流程通过：登录、分类/景点/票种/库存、用户管理、订单查询、部分/全部核销、统计、月报、审计和退出。
- [x] 16. 普通用户直接调用管理员景点查询被拒绝，原有全量授权边界测试同时通过。
- [x] 17. M10 并发和完整订单套件再次证明库存不超卖、不为负且三库存桶平衡。
- [x] 18. 日期前且未核销的已支付订单可模拟退款，真实测试记录退款并恢复库存。
- [x] 19. 过期日期、已完成或已核销订单不能退款，退款边界单元测试通过。
- [x] 20. 评论、简介、推荐分、推荐理由、热门排行名称和审计组合查询均通过 M10 自动化与真实数据库复跑；最终 Swing 视觉冒烟仍待 M11。
- [ ] 21. 服务/数据库端到端测试已证明多次退出重登后的档案、订单状态和评论一致；真实 Swing 刷新、滚动和重登视觉操作仍待 M11 手工确认。
- [x] 22. README 已与现有启动脚本、安全测试库命令和 M10 测试结果校准；`DocumentationConsistencyTest` 固定关键入口。
- [x] 23. 用户手册已与当前页签、按钮、购票估算/服务端金额和管理员订单只读策略校准；一致性回归通过。
- [x] 24. MySQL E-R 与 MongoDB 集合文档已按空库实测结构、生命周期字段、对象数量和 Day09 索引校准；一致性回归通过。
- [x] 25. `PROGRESS.md` 已记录 M11 最终默认与真实数据库测试结果。
- [x] 26. M11 检查点前逐文件复核，工作区仅包含本里程碑文档、Mongo 脚本保护和对应回归；提交后再次确认状态。
- [x] 27. M11 最终扫描未发现密码、Token、私钥或开发机绝对路径；`db.properties` 与 `target/` 保持忽略。
- [x] 28. 最终报告已包含修复、功能、迁移、测试、手工 Swing、限制、启动、账号和五分钟演示，并如实标记未完成项。

## M10 后待验收项

- 普通用户和管理员已在同一真实测试库执行两套完整端到端流程，临时 MySQL/MongoDB 数据均在测试结束清理。
- 推荐分、审计组合查询、热门排行名称、评论和简介已自动化复跑；仍需 M11 的真实 Swing 视觉冒烟。
- 所有真实数据库自动测试继续只允许连接 `scenic_ticket_test`；生产名数据库不会被用于测试。
- Mongo 初始化/迁移逻辑已由 Java Driver 和官方 `mongosh` 在空隔离库双重验证；脚本硬编码切换业务库的问题已关闭。
