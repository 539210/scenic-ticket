# 景点售票系统稳定化最终报告

报告日期：2026-07-15

状态：当前功能、提交文档和默认自动化回归已按 2026-07-15 版本校准；此前真实数据库验收结果保留。真实 Swing 手工清单仍需由桌面执行者签字，因此本报告不将其伪报为已完成。

## 1. 项目目标与技术栈

项目将课程原始工程完善为可稳定演示的 Java Swing 景点售票系统，保留 Java 21、Swing、Maven、JDBC、MySQL、MongoDB、HikariCP、JUnit 5、SLF4J/Logback 和 BCrypt，并保留课程要求的五张原表及四个 MongoDB 集合。

## 2. 主要功能

- 用户注册、登录、退出、档案和基于 actor 回查的权限控制。
- 分类树、景点、UTF-8 详情、拖拽图片导入和开放信息维护。
- 多票种、未来游玩日期、每日 available/reserved/sold 库存。
- 待支付、主动支付、取消、过期、退款状态机；历史核销数据与退款限制兼容保留。
- 已支付资格评论、每用户每景点唯一修改、评分卡片展示；评论标签已移除。
- 按平均评分推荐、月报及订单明细钻取、热门排行、用户报告和综合统计。
- 用户/类型/级别/日期/关键词/条数组合审计及汇总、趋势。
- 管理员用户查询、详情、启禁和角色维护，保护自身和最后管理员。

## 3. 已知四问题修复

| 问题 | 根因 | 处理与证据 |
| --- | --- | --- |
| 评论显示错误 | 历史中文已损坏成问号，且旧渲染字段不全 | 独立评论 DTO/页签、UTF-8、混合 ID、脱敏/评分/双时间；不可逆内容显示明确降级提示 |
| 景点简介错误 | 历史正文损坏及异步选择残留 | 独立简介页签、选择代次保护、结构化编辑和数值/字符串 ID 兼容 |
| 图片只能看到地址 | 简介把 MongoDB `images` 数组格式化成 URL 文字 | 后台异步下载第一张有效图片、等比缩放后直接显示；无图、失效或非图片响应时完全隐藏图片区 |
| 推荐评分不显示 | UI 与评分聚合结果未统一展示 | 景点浏览页固定“评分”列，显示游客平均评分（5 分制）；普通查询中无评分显示“-” |
| 审计条件查询异常 | 条件模型不完整且页面结果路由混乱 | `AuditLogQuery` 完整组合、四结果页、日期全天与清空/刷新回归 |

## 3.1 2026-07-15 交互与数据修订

| 项目 | 修订内容 |
| --- | --- |
| 推荐 | 删除“为你推荐”等前台选项，统一按评论平均分进行评分推荐，表格显示 5 分制评分 |
| 评论 | 删除标签字段、标签输入、标签统计和标签审计；移除测试数据中的“第 N 条评论”前缀 |
| 景点详情 | 简介在选中景点时预取并缓存；图片采用拖拽导入，本地校验后直接显示，缺图时隐藏图片区 |
| 后台编辑 | 景点基础信息、详情、图片和开放信息统一由一个保存按钮提交；减少筛选和编辑区的无效空白 |
| 日期 | 库存、审计等所有业务日期使用可切换月份的月历选择器，不再接受依赖格式的手工输入 |
| 票务 | 票种条件留空可查询全部；创建景点后自动创建成人票和未来 31 天库存 |
| 报表与审计 | 热门排行/综合汇总采用稳定排序；当前功能按钮绿色高亮；月度订单行可打开订单明细 |

详细复现和根因见 `docs/KNOWN_BUG_REPRODUCTION.md`。

## 4. 其他关键缺陷处理

- 关闭服务层管理员权限绕过和任意订单状态改写两个 P0。
- 关闭待支付缺失、无日期库存、无退款、无核销、跨库审计误报、测试库不隔离和 Mongo 初始化破坏性风险等七个 P1。
- 修复评论重复、字段缺失、热门排行只显示 ID、数据库对象无调用、旧异步请求覆盖、订单按钮误导、空筛选 NPE、点击后选择漂移、价格估算显示旧值等 P2/P3。
- `AppFrame` 从页面巨石拆到 788 行，仅负责窗口/服务装配和对话框编排。

## 5. 数据库与迁移

MySQL 全新安装包含 10 表、12 外键、2 视图、2 存储过程、2 触发器。课程五表/字段保留，新增迁移记录、票种、每日库存、退款和核销表。Day08 旧结构升级实测保留历史用户、档案和订单。

MongoDB 保留 `action_logs`、`comments`、`item_details`、`system_logs`，增加兼容 ID 迁移、唯一/复合/文本索引。初始化脚本遇到既有受管集合会拒绝破坏性执行。2026-07-15 已加入并执行 `mongodb_day11_remove_comment_tags.js`，清理历史评论及评论审计中的标签字段，不修改评分、正文和时间。

详细正向迁移、兼容和回滚见 `docs/DATABASE_MIGRATIONS.md`，结构见 `docs/MySQL_ER图.md` 与 `docs/MongoDB集合设计文档.md`。

## 6. 一致性和降级策略

- MySQL 核心订单/库存操作使用单连接显式事务与行锁。
- Mongo 审计失败不回滚已提交核心事实，UI 明确提示业务成功但审计失败。
- 新景点 Mongo 详情失败时尝试自动下架 MySQL 景点，避免无详情继续售卖。
- 跨库读取缺失时返回明确空状态/降级内容，不伪造 Mongo 与 MySQL 的原子事务。

## 7. 测试结果

| 验证 | 结果 |
| --- | --- |
| Java 21 默认 `mvn test`（2026-07-15） | 182 tests，0 failures，0 errors，2 skipped |
| Java 21 真实数据库完整套件 | 172 tests，0 failures，0 errors，1 skipped |
| MySQL 空库安装 | 10 表、12 外键、2 视图、2 过程、2 触发器、非法库存 0 |
| Day08 升级 | 历史用户/档案/订单保留，数据库对象完整 |
| 普通用户/管理员完整流程 | 真实 `scenic_ticket_test` 串联通过并清理临时数据 |
| 库存并发 | 不超卖、不为负、三库存桶平衡 |

唯一真实套件跳过项是显式开启的 Fake DAO 压力测试，不属于真实数据库功能。M10 基线详见 `docs/M10_REAL_DATABASE_ACCEPTANCE.md`；M11 另外加入 5 项文档一致性回归。

## 8. Swing 自动与手工验收

所有主要页面、弹窗、表格、按钮策略、输入、空状态、异步会话与最新请求均有自动测试。真实视觉、滚动、快速连点、断库恢复和完整鼠标流程必须按 `docs/M11_SWING_MANUAL_ACCEPTANCE.md` 执行。

当前手工结果：待桌面执行者签字，未伪报通过。

## 9. 启动与账号

1. 复制 `src/main/resources/db.properties.example` 为未提交的 `db.properties` 并填写本机凭据。
2. 按 README 顺序安装 MySQL/MongoDB 脚本。
3. 执行 `.\start-app.cmd --check`，再执行 `.\start-system.cmd` 或 `.\start-app.cmd`；Windows 脚本可自动发现注册表登记的 IntelliJ 内置 Maven。
4. 初始化管理员：用户名 `kongsc`，密码 `ksc123456`。

## 10. 五分钟演示

演示顺序为：架构 → 用户浏览/评分推荐/购票 → 待支付/支付/退款/评论 → 管理员景点/库存/用户 → 月度订单明细与统计审计 → 测试结果。逐分钟脚本见 `docs/FIVE_MINUTE_DEMO.md`，常见问题见 `docs/DEFENSE_QA.md`。

## 11. 已知限制

- 历史 `????` 数据只有在 ID、序号及业务字段能严格匹配可信初始化模板时才会备份后自动恢复；无法证明来源的真实内容仍不猜测，需管理员按原资料重填。
- MongoDB 与 MySQL 是显式最终一致/降级设计，不提供分布式事务。
- Mongo JS 脚本已通过官方 `mongosh 2.9.2` 在空隔离库实跑；脚本现在依赖连接 URI 选库并只允许两个约定库名，运行前仍应确认 URI。
- 真实 Swing 手工清单尚未签字前，刷新/重登视觉、窗口布局、快速连点和断库恢复不能声明最终完成。
- 拖拽导入的景点图片保存在当前 Windows 用户目录 `.scenic-ticket/images`，提交项目源码时不应把该本地图片缓存当作数据库初始化数据。

## 12. 安全与版本控制

测试保护器只允许两个数据库均为 `scenic_ticket_test`；`db.properties`、Token、私钥和机器绝对路径不进入提交。M0–M10 均建立 Git 检查点并同步远端；最终 M11 提交前再次执行全量回归、敏感信息扫描和工作树核对。
