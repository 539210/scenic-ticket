# Swing UI 操作矩阵

更新时间：2026-07-13

状态：`未测` / `静态检查` / `自动测试` / `手工通过` / `失败` / `环境阻塞`

## 页面与控件清单

| 页面 | 控件/操作 | 普通用户 | 管理员 | 当前状态 | 主要检查点/发现 |
| --- | --- | --- | --- | --- | --- |
| 登录 | 用户名、密码、登录、注册入口 | 是 | 是 | 自动测试/静态检查 | 独立 `LoginPanel` 覆盖成功、禁用、服务异常、注册导航及密码清理；忙碌遮罩阻止重复点击，真实连点待 M10/M11 |
| 注册 | 用户名、密码、邮箱、手机号、注册、返回 | 是 | 是 | 自动测试/静态检查 | 独立 `RegisterPanel` 覆盖完整参数、成功/失败、返回登录及密码清理；服务测试覆盖常规校验和 Mongo 审计降级 |
| 首页 | 档案、景点、订单、报表、刷新、管理员入口 | 是 | 是 | 自动测试/静态检查 | 独立 `HomePanel` 测试覆盖身份摘要、普通/管理员入口、导航和刷新状态；真实页面重建待 M10/M11 |
| 顶部会话 | 退出登录 | 是 | 是 | 自动测试/静态检查 | 写 LOGOUT；审计失败显示警告；会话代次阻止旧任务跨账号回写 |
| 个人档案 | 刷新、姓名、证件号、地址、简介、保存 | 是 | 是 | 自动测试/静态检查 | 独立 `ProfilePanel` 测试覆盖加载、表单填充、当前用户绑定和保存；忙碌遮罩防重复保存，真实断库恢复待 M10/M11 |
| 景点浏览 | 预设关键词、自定义词、类型、查询、重置 | 是 | 是 | 自动测试/静态检查 | 独立 `ScenicBrowsePanel` 测试覆盖查询结果、类型和折后价；分类加载在 EDT 成功回调更新缓存，真实断库恢复待 M10/M11 |
| 景点浏览 | 推荐菜单：为你推荐/热门/高分 | 是 | 是 | 自动测试/静态检查 | 独立组件测试覆盖推荐分 0–100 边界与选中概览理由；空历史使用热门/最新补充 |
| 景点浏览 | 景点表选择、简介、评论、购买、发表评论 | 是 | 是 | 自动测试/静态检查 | 测试覆盖上下架购买按钮联动；概览、简介、评论独立页签，切换清空旧内容并丢弃过时详情回调 |
| 购买弹窗 | 票种/日期、数量、付款方式、服务端金额、创建待支付/取消 | 是 | 是 | 自动测试/静态检查 | 仅显示有库存选项；创建后提示 15 分钟内到订单页主动支付 |
| 评论弹窗 | 评分、正文、逗号分隔标签、确认/取消 | 是 | 是 | 自动测试/真实跨库测试 | 服务层重查支付资格；重复提交更新唯一评论；Mongo 审计失败时保存结果不回滚并给出警告 |
| 我的订单 | 订单号、状态、查询、刷新、重置 | 是 | 是 | 自动测试/静态检查 | 独立 `OrderPanel` 测试固定普通用户强制当前 ID、管理员空用户筛选不拆箱崩溃；票种和游玩日期由 `OrderTableModelsTest` 覆盖 |
| 我的订单 | 选择订单、确认支付、取消待支付、申请退款 | 是 | 是 | 自动测试/静态检查 | 页面测试覆盖待支付按钮组合、管理员不可操作他人订单、退款原因传递及点击时订单 ID 快照；专用事务服务继续执行最终资格校验 |
| 后台-景点 | 关键词、类型、查询、刷新、重置、新增 | 否 | 是 | 自动测试/静态检查 | 独立 `ManagementPanel` 测试覆盖分类缓存、列表/空状态和选择按钮；服务层授权与真实库 CRUD 通过，真实连点待 M10 |
| 后台-景点 | 名称/分类、价格/优惠、上下架、简介/图片/扩展属性 | 否 | 是 | 自动测试/静态检查 | 页面测试覆盖详情加载保护、名称分类和价格优惠参数/消息；服务测试覆盖完整编辑与 Mongo 失败补偿 |
| 后台-分类 | 分类名、父级、新增、修改、刷新、层级路径 | 否 | 是 | 自动测试/静态检查 | 页面测试覆盖树路径、父级回填、CRUD 和直接自父级拒绝；服务层禁止跨多级循环 |
| 后台-用户 | 查询、详情、档案、启禁、角色、概况 | 否 | 是 | 自动测试/静态检查 | 独立 `UserManagementPanel` 测试覆盖完整筛选、本人按钮禁用、目标快照、变更消息和 Mongo 降级；服务测试覆盖最后管理员、越权和并发规则 |
| 后台-票种 | 按景点查询、新增、编辑、上下架 | 否 | 是 | 自动测试/静态检查 | 独立 `TicketInventoryPanel` 测试覆盖选择回填、按钮状态、价格/优惠参数、CRUD 刷新和结果消息；服务层管理员校验与真实数据库 CRUD 已覆盖 |
| 后台-库存 | 日期范围查询、票种、总库存维护 | 否 | 是 | 自动测试/静态检查 | 页面测试覆盖日期范围快照、库存桶/版本行与保存后消息；服务/真实库覆盖行锁、已承诺下限和 20 线程防超卖 |
| 后台-核销 | 订单ID、数量、备注、核销、记录查询 | 否 | 是 | 自动测试/静态检查 | 独立 `AdmissionPanel` 测试覆盖参数快照、记录字段、空结果和刷新后保留业务消息；服务测试覆盖管理员、当天、状态、累计数量、完成、审计降级和退款排斥 |
| 报表 | 年、月、用户 ID、月报/热门/用户/综合/刷新 | 是 | 是 | 自动测试/静态检查 | 独立 `ReportPanel` 测试覆盖热门名称/状态、当前页刷新、空结果提示及普通用户本人/管理员目标用户绑定；服务层继续强制权限 |
| 系统审计 | 用户 ID、类型、级别、日期、关键词、limit、查询、清空 | 否 | 是 | 自动测试/静态检查 | 独立 `AuditPanel` 测试覆盖完整筛选 DTO、结束日毫秒边界、中文结果和清空重查；权限在服务层 |
| 系统审计 | 汇总、趋势、用户操作、刷新、表格滚动 | 否 | 是 | 自动测试/静态检查 | 测试覆盖汇总结果及刷新保持当前页；三类聚合复用日期范围，真实滚动视觉冒烟待 M10 |

## 通用场景矩阵

每个有输入/提交/查询的操作都要覆盖：

| 场景 | 当前覆盖 | 后续动作 |
| --- | --- | --- |
| 正常输入 | 部分 | 为新业务补服务与集成测试 |
| 空输入 | 自动测试/静态检查 | `UiInputParsers` 统一必填/可选输入；服务层继续校验业务必填字段 |
| 非法输入 | 自动测试 | ID、整数、金额、ISO 日期以及业务状态/角色边界均有拒绝路径 |
| 边界值 | 自动测试/真实数据库测试 | 日期今天、库存 0/1、负金额、折扣 0/100、审计结束日毫秒边界已覆盖；Swing 可见提示待手工复核 |
| 重复点击 | 部分自动测试/静态检查 | M9 忙碌 glass pane 会消费鼠标、滚轮和键盘事件；订单、支付、退款、核销仍需真实桌面快速点击复核 |
| 快速连续点击 | 部分自动测试/静态检查 | 后台任务统一进入 `SwingTaskRunner`，并发业务边界主要由服务层事务保护；Swing 手工连点留待 M10/M11 |
| 查询无结果 | 静态检查 | 表格空状态手工验收 |
| 数据库断开 | 部分自动测试 | 数据库/权限/通用异常已有中文分类提示；真实断库恢复仍待 M10/M11 |
| MongoDB 写失败 | 未测 | MySQL 成功后的告警/补偿 |
| MySQL 写失败 | 部分自动测试 | 扩展库存/支付/退款/核销回滚 |
| 刷新页面 | 部分自动测试/静态检查 | 同名旧请求不能覆盖新请求；选择/详情一致性与滚动位置仍需手工验收 |
| 退出后重新登录 | 自动测试/静态检查 | 会话代次使旧异步回调失效；完整 Swing 手工点击留待 M10/M11 |
| 普通用户越权 | 自动测试 | 服务层 actor 回查并拒绝管理员、审计、系统统计和他人私有数据调用 |
| 管理员权限 | 自动测试 | 自身禁用/角色修改和最后有效管理员规则均拒绝 |
| 数据持久化 | 未测 | 真实测试库重启/刷新验证 |
| UI 与数据库一致 | 未测 | MySQL/MongoDB/表格字段逐项验收 |

## 手工 Swing 验收说明

当前环境没有自动点击 Swing 的工具链，且课程 DOCX 渲染依赖缺失。不会把静态检查或单元测试写成“手工通过”。M10/M11 将生成普通用户和管理员两套逐步冒烟清单，由可实际看到桌面的执行者完成并记录。
